{
  "name": "imposter-zero",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "typecheck": "tsc --build --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
